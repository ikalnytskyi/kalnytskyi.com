language: python
python: "3.6"

install: pip install git+https://github.com/ikalnytskyi/holocron.git@e58d246
script: holocron run compile

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: _site
  fqdn: kalnytskyi.com
  on:
    branch: master
