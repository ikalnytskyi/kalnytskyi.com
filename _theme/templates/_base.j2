<!doctype html>
<head>
{% block head %}
  <meta charset="{{ encoding }}">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <link rel="stylesheet" href="/static/style.css">

  {% if feedurl %}
    <link rel="alternate" type="application/atom+xml" href="{{ feedurl }}" title="{{ site.title }}">
  {% endif %}

  <title>{% block title %}{{ site.title }}{% endblock %}</title>
{% endblock %}

<body>
  <aside>
    <div class="container">
      {% if 'sidebar.photo' in theme %}
        <img class="photo" src="{{ theme.sidebar.photo }}">
      {% endif %}

      {% if 'sidebar.about' in theme %}
        <span class="about">{{ theme.sidebar.about }}</span>
      {% endif %}

      <nav>
        {% for name, url in theme.get('navigation', []) %}
          <span><a href="{{ url }}">{{ name }}</a></span>
        {% endfor %}
      </nav>
    </div>
  </aside>

  <main>
    {% block content %}
    {% endblock %}
  </main>
