<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="{{ encoding | default("UTF-8") }}" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <link rel="icon" href="/static/favicon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/static/apple-touch-icon.png">
    <link rel="manifest" href="/static/manifest.webmanifest">
    <link rel="stylesheet" href="/static/style.css" />
    <link rel="stylesheet" href="/static/pygments.css" />
    <link rel="canonical" href="{{ item.absurl }}" />
    <link rel="alternate" type="application/atom+xml" href="{{ metadata.feedurl }}" title="{{ metadata.title }}" />

    {% block head %}{% endblock %}

    <title>{% block title %}{{ metadata.title }}{% endblock %}</title>
</head>
<body>
  <div class="banner">
    Русский военный корабль, иди нахуй.
    <a href="https://www.comebackalive.in.ua/donate">Help Ukrainian army →</a>
  </div>

  <aside>
  {% if theme.sidebar.photo %}
    <img class="about-pic" src="{{ theme.sidebar.photo }}" alt="Ihor's photo" />
  {% endif -%}

  {% if theme.sidebar.about %}
    <span class="about-txt">{{ theme.sidebar.about }}</span>
  {% endif %}

    <div class="nav-wrapper">
    <nav>
    {% for name, url in theme.get('navigation', []) %}
      <span><a href="{{ url }}">{{ name }}</a></span>
    {% endfor %}
    </nav>
    </div>
  </aside>

  <main>
    {% block content %}
    {% endblock %}
  </main>
</body>
